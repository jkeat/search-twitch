import { paginatorButtonDisableCheck } from '../../../helpers/paginator-button-disable-check';
import { module, test } from 'qunit';

module('Unit | Helper | paginator button disable check');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = paginatorButtonDisableCheck([42]);
  assert.ok(result);
});
