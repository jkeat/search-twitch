import { moreThanOnePage } from '../../../helpers/more-than-one-page';
import { module, test } from 'qunit';

module('Unit | Helper | more than one page');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = moreThanOnePage([42]);
  assert.ok(result);
});
