import { linkToProfile } from '../../../helpers/link-to-profile';
import { module, test } from 'qunit';

module('Unit | Helper | link to profile');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = linkToProfile([42]);
  assert.ok(result);
});
